import styled from "styled-components";

export const Button = styled.button`
  font-family: inherit;
  font-size: 12px;
  width: 100%;
  display: block;
  text-decoration: none;

  padding: 0.8em;
  &:hover {
    cursor: pointer;
  }

`;